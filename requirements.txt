# PINNED to the last known-good deploy set (06 Jul 2026, Python 3.14 on Streamlit Cloud).
# Unpinned ">=" requirements let every rebuild pull brand-new wheels: the 13 Jul 2026
# rebuild picked up pyarrow 25.0.0 (released 10 Jul) and the app started dying with a
# Segmentation fault at boot — matching platform-wide reports that began 2026-07-10.
# If you bump anything here, bump ONE package at a time and watch the deploy.
streamlit==1.59.0
pyarrow==24.0.0
pandas==3.0.3
numpy==2.5.1
yfinance==1.5.1
curl-cffi==0.15.0
plotly==6.9.0
requests==2.31.0
feedparser==6.0.12
vaderSentiment==3.3.2
beautifulsoup4==4.15.0
python-dateutil==2.9.0.post0
rich==15.0.0
streamlit-autorefresh==1.0.1
fyers-apiv3==3.1.14
pyotp==2.10.0
